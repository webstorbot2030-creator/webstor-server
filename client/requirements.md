## Packages
framer-motion | For smooth animations, modal transitions, and the news ticker slider
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
The application requires Right-to-Left (RTL) layout support.
Authentication is phone-based.
The design uses a heavy glassmorphism aesthetic on a dark gradient background.
Admin panel requires role-based access control.
